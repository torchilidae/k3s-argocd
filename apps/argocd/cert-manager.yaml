apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: certmanager-argocd
  namespace: argocd
spec:
  secretName: argocd-cert
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
  dnsNames:
    - "*.k3s.tsphotoclicks.net"
